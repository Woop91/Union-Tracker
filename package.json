{
  "name": "union-steward-dashboard",
  "version": "4.4.1",
  "description": "Modular Union Steward Dashboard for Google Sheets",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "lint": "eslint src/**/*.gs --ext .gs",
    "lint:fix": "eslint src/**/*.gs --ext .gs --fix",
    "test": "npm run lint && npm run build",
    "watch": "node build.js --watch",
    "clean": "node build.js --clean",
    "manifest": "node build.js --manifest",
    "deploy": "npm run test && npm run manifest && clasp push",
    "open": "clasp open"
  },
  "keywords": [
    "google-apps-script",
    "google-sheets",
    "union",
    "grievance",
    "dashboard"
  ],
  "author": "Dashboard Team",
  "license": "MIT",
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "eslint": "^8.57.0",
    "eslint-plugin-googleappsscript": "^1.0.5"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Woop91/MULTIPLE-SCRIPS-REPO"
  }
}
